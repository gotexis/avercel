{
  "name": "avercel",
  "version": "0.1.0",
  "description": "Agent-Vercel: opinionated Vercel CLI wrapper — patches env quirks, adds env auditing, blocked environments, and command guardrails",
  "main": "dist/index.js",
  "bin": {
    "avercel": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js",
    "pretest": "npm run build",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build && npm test"
  },
  "keywords": [
    "vercel",
    "cli",
    "env",
    "wrapper",
    "devtools"
  ],
  "author": "gotexis",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gotexis/avercel.git"
  },
  "bugs": {
    "url": "https://github.com/gotexis/avercel/issues"
  },
  "homepage": "https://github.com/gotexis/avercel#readme",
  "engines": {
    "node": ">=18"
  },
  "peerDependencies": {
    "vercel": "*"
  },
  "peerDependenciesMeta": {
    "vercel": {
      "optional": true
    }
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ]
}
